

![redis](https://img.shields.io/badge/GPU-linux-red)

# PrismGPU 
On building

## step 
<dl>
  <dt>step 1</dt>
  <dd>you can `make pre` to build start disk amd cp prism source file to qemu,but you need to write you qemu prject path and down.</dd>
  <dt>step 2</dt>
  <dd>you can `make qemu` to build qemu.but if you want to debug qemu you need add `DEBUG=1`</dd>
  <dt>step 3</dt>
  <dd>you can `make run` to start qemu.but one thing i need to recomand you this is when you frist start qemu you must add `BOOT=1`</dd>
</dl>
